.. _sec_install:

Installation
============

.. automodule:: motile
   :noindex:

.. code-block:: bash

    conda create -n motile -c funkey pylp
    conda activate motile
    pip install motile

That's all.
